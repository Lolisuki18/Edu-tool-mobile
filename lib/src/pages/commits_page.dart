import 'package:flutter/material.dart';

class CommitsPage extends StatelessWidget {
  const CommitsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Commits (GitHub) - Thống kê đóng góp',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
